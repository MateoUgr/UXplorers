
  # Convert to React

  This is a code bundle for Convert to React. The original project is available at https://www.figma.com/design/Ybo9KJVgMOCKioEcPrmwYH/Convert-to-React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  